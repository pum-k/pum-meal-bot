const { handleIngredient } = require('./handleIngredient')
const handleGram = require('./handleGram')
const handleHistory = require('./history')
const handleReport = require('./report')
const handleRemove = require('./remove')
const handleMeals = require('./meals')

module.exports = { handleIngredient, handleGram, handleHistory, handleReport, handleRemove, handleMeals }
